{
  "npm": {
    "publish": false
  },
  "hooks": {
    "after:bump": "code CHANGELOG.md && read -p 'Edit changelog, then press Enter to continue...' && npm version ${version} --workspaces --no-git-tag-version"
  }
}