{
  "github": {
    "publish": false
  },
  "npm": {
    "publish": false
  },
  "hooks": {
    "after:bump": "npm version ${version} --workspaces --no-git-tag-version && open CHANGELOG.md && read -p 'Edit changelog, then press Enter to continue...'"
  }
}